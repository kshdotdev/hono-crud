/**
 * @deprecated This module has moved to `src/utils/etag.ts`.
 * Existing imports continue to work via this re-export shim.
 */
export { generateETag, matchesIfMatch, matchesIfNoneMatch } from '../utils/etag';
